---
date: <% tp.date.now("YYYY-MM-DD") %>
scope: <% tp.system.suggester(["transverse", "espace_opti", "vp", "socialflow", "supply_chain_tower"], ["transverse", "espace_opti", "vp", "socialflow", "supply_chain_tower"]) %>
tags: [blocker]
---

## <% tp.date.now("YYYY-MM-DD") %> — <% tp.file.title %>

- **Scope** : `<% tp.frontmatter.scope %>`
- **Tags** : #blocker #<% tp.frontmatter.scope %> #recurrence-1
- **Première occurrence** : <% tp.date.now("YYYY-MM-DD") %>
- **Récurrences** : 1
- **Symptôme observé** :
- **Cause racine** :
- **Mitigation actuelle** :
- **Mitigation cible** :
- **Coût cumulé** :
- **Statut** : OPEN
